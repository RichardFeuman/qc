object Compositions extends App {
  def add(x: Int, y: Int): Int = x + y
  def mul(x: Int, y: Int): Int = x * y
  def sub(x: Int, y: Int): Int = x - y



  // здесь должна быть композиция функций с именем composition3

  /*def composition3(arg1, arg2, arg3, arg4): Int = {
    add(arg2, sub(arg4, mul(arg1, arg2) ))

  }

  val res = composition3(arg1, arg2, arg3, arg4) // раскомментируйте вызов функции composition3
  */
}
